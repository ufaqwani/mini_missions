{
  "name": "mission-tracker",
  "version": "1.0.0",
  "description": "Multi-user task management app",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
